class logo {
  constructor(color, text, textColor) {
    this.color = color;
    this.text = text;
    this.textColor = textColor;
  }
  setColor(color) {
    this.color = color;
  }
  setText(text) {
    this.text = text;
  }
  settextColor(textColor) {
    this, textColor;
  }
}
module.exports = shapes;
